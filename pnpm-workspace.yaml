packages:
  - apps/*
  - packages/*
  - examples/*

allowBuilds:
  esbuild: true
  sharp: true
  workerd: true

overrides:
  lodash: 4.18.1
  # Miniflare still pins 0.34.x; keep the patched libvips bundle until upstream catches up.
  sharp: 0.35.3
  uuid: 11.1.1
